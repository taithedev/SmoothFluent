# SmoothFluent Components

This folder mirrors the component-oriented organization used by Fluent-modded while keeping SmoothFluent independently implemented.

Components include Window, TitleBar, Tab, Section, Element, Notification, Dialog, Button, Textbox, and Acrylic surface.

The public API remains exposed through src/Fluent.lua so users can still use a single ModuleScript.
